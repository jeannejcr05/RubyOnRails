<p id="notice"><%= notice %></p>

<h1>News</h1>

<div class="cols-1 cols-md-12 g-12 main">
  <% @news.each do |news| %>
    <div class="col-12">
      <div class="title"> <%= news.title%></div>
      <div class="date">  <%= news.date %></div>
      <%= link_to 'Show', news %>
      <%= link_to 'Edit', edit_news_path(news) %>
      <%= link_to 'Destroy', news, method: :delete, data: { confirm: 'Tu es sûr(e) ?' } %>
    </div>
  <% end %>
</div>

<%= link_to 'New News', new_news_path %>
