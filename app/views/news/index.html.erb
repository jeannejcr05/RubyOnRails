<p id="notice"><%= notice %></p>

<h1>News</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Date</th>
      <th>Mood</th>
      <th>Doing</th>
      <th>Picture</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>

  <div class="row row-cols-1 row-cols-md-4 g-4">
<% @news.each do |news| %>
<td><div class="col mb-4"></td>
<td><div class="title"> <%= news.title%></td>
<td><div class="date">  <%= news.date %></td>
<td><%= link_to 'Show', news %></td>
<td><%= link_to 'Edit', edit_news_path(news) %></td>
<td><%= link_to 'Destroy', news, method: :delete, data: { confirm: 'Tu es sûr(e) ?' } %></td>
</div>
</div>
</div>
<% end %>
</div>

  </tbody>
</table>

<br>

<%= link_to 'New News', new_news_path %>
